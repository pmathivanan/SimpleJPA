create database development;
use development;
create table user_info(id INT NOT NULL AUTO_INCREMENT,salutation varchar(100),firstName varchar(100),lastName varchar(100),middleName varchar(100),PRIMARY KEY (id));
